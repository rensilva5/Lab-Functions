/*-- LAB 5
 - Check if input variable is a number or not
 - Function `isNumber()` checks if input variable is a number by using isNaN()in-built
   JavaScript function. Read more about isNan() from w3schools.com/jsref/jsref_isnan.asp.
 - Print "Variable is not a number" if isNaN() returns true.
 - Else print "Variable is a valid number" if isNaN() returns false. */

 function isNumber() {
    if ()) {
        
        return

    } else {

        return
    }

 }
isNumber()
